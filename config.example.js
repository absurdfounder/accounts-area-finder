// Copy to config.js and fill in your Google Cloud OAuth Web client ID.
// Client IDs are public; restrict them with Authorized JavaScript origins.
window.APP_CONFIG = {
  GOOGLE_CLIENT_ID: "YOUR_CLIENT_ID.apps.googleusercontent.com",
  // Optional. People API works with OAuth access token alone; API key is unused by default.
  GOOGLE_API_KEY: "",
};
